WITH
 -- 1. Define specific members for each Quarter of 2024
 -- FIX: Use direct member path [Year].[Quarter] to avoid key resolution errors
 MEMBER [Measures].[Q1 2024] AS (
   [dim_timemonth].[Time].[2024].[1],
   [Measures].[Exceed Days (any)]
 )
 MEMBER [Measures].[Q2 2024] AS (
   [dim_timemonth].[Time].[2024].[2],
   [Measures].[Exceed Days (any)]
 )
 MEMBER [Measures].[Q3 2024] AS (
   [dim_timemonth].[Time].[2024].[3],
   [Measures].[Exceed Days (any)]
 )
 MEMBER [Measures].[Q4 2024] AS (
   [dim_timemonth].[Time].[2024].[4],
   [Measures].[Exceed Days (any)]
 )


SELECT
 -- 2. Select the calculated quarters on Columns
 {
   [Measures].[Q1 2024],
   [Measures].[Q2 2024],
   [Measures].[Q3 2024],
   [Measures].[Q4 2024]
 } ON COLUMNS,


 -- 3. Select the specific set of Countries on Rows
 {
   [dim_city].[Geo].[country_name].&[Russia],
   [dim_city].[Geo].[country_name].&[Turkey],
   [dim_city].[Geo].[country_name].&[Austria],
   [dim_city].[Geo].[country_name].&[Germany]
 } ON ROWS


FROM [AirQ Cube]
